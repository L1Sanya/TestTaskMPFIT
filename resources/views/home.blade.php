<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NASAK DURAK</title>
    <style>
        :root {
            --bg-black: #000000;      <!-- Абсолютно чёрный -->
            --card-gray: #121212;     <!-- Тёмные карточки -->
            --text-light: #e0e0e0;    <!-- Светлый текст -->
            --accent-purple: #7e57c2; <!-- Акцентный цвет -->
        }
        body {
            background-color: var(--bg-black);
            color: var(--text-light);
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 20px;
            min-height: 100vh;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .card {
            background-color: black;
            border-radius: 8px;
            padding: 30px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
            border-left: 4px solid var(--accent-purple);
        }
        h1 {
            color: deeppink;
            text-align: center;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="card">
        <h1>🚀 NASAK PRIVET DURAK</h1>
    </div>
</div>
</body>
</html>